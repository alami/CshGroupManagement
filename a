var mvcBuilder = /*services.AddMvc(option =>
            {
                option.EnableEndpointRouting = false;  //[ <---- МОЯ РЕДАКЦИЯ: иначе не запускается]
            });
            var builder =*/ services.AddMvcCore();

            //builder.AddApiExplorer(); --for Swagger
            //builder.AddAuthorization(); --future
            //AddDefaultFrameworkParts(builder.PartManager);  --Helper...

            // Order added affects options setup order

            // Default framework order
            // builder.AddFormatterMappings();
            // builder.AddViews();
            // builder.AddRazorViewEngine();
            // builder.AddRazorPages();
            // builder.AddCacheTagHelper();

            // +1 order
            //builder.AddDataAnnotations(); // +1 order

            // +10 order
            builder.AddJsonFormatters();

            //builder.AddCors();
            return services;